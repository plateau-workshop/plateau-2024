jekyll serve
bundle exec jekyll serve

When you're ready to set up the DNS for this year.

Change the Settings > Github Pages > Custom Domain to for example <curryear>.plateau-workshop.org

Update DNS settings with a CNAME record that maps for example <curryear> to schasins.github.io

Update https://github.com/plateau-workshop/plateau-root-redirect to point to the subdomain for the current year
