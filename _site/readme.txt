jekyll serve
bundle exec jekyll serve

When you're ready to demote this one from being the top-level workshop (to a subdomain).

Change the Settings > Github Pages > Custom Domain to for example 2020.plateau-workshop.org

Update DNS settings with a CNAME record that maps for example 2020 to schasins.github.io
